let cards_player = []
let cards_machine = []
// partidas = toParse(list_card.length/2)
function sortCards() {
  // for list_card[index]
  // if (checkSort(index))
  // else
}
function sortCards(index) {
  // let checkPlayer = cards_player
  // let checkMachine = cards_
}
